# binaries / build
/bin/
/gofer
*.exe
*.test
*.out
dist/

# tooling
.idea/
.vscode/
.DS_Store

# VHS TUI capture artifacts (see vhs/, scripts/tui-vhs.sh)
# vhs/out/ holds per-run renders (GIF + PNG) and the built harness — both
# transient. The PNG key-frames are mirrored (CI-authoritative) into the
# tracked vhs/snapshots/ baseline, which is intentionally NOT ignored so
# main<->feature image diffs work.
vhs/.bin/
vhs/out/

# local worktrees convention
worktrees/

# local Claude Code overrides
.claude/settings.local.json
